fastapi==0.136.3
# Épinglée bien que fastapi la tire déjà : fastapi 0.136.3 déclare
# `starlette>=0.46.0` SANS borne haute, donc `pip install -r requirements.txt`
# résout aujourd'hui starlette 1.6.0 et non la 1.2.0 du poste de dev — une
# version subie, pas choisie. Or on dépend directement de ses internes :
# `_drop_module_routes` filtre `app.router.routes` à la main, faute d'API
# publique de démontage (cf. docs/limite-demontage.md).
# Mesuré avant de l'épingler : 0.136.3 + starlette 1.6.0 passe les 207 tests.
# L'épinglage sert donc la reproductibilité, pas une correction — c'est
# `fastapi` qui porte la limite, pas `starlette`.
starlette==1.2.0
uvicorn==0.48.0
# Implémentation WebSocket d'uvicorn — sans elle, TOUT `/ws/*` est mort.
# `uvicorn` seul ne sait pas parler WebSocket : il ne le sait que si `websockets`
# ou `wsproto` est importable (`uvicorn/protocols/websockets/auto.py`, testé dans
# cet ordre). Sinon `_should_upgrade()` renvoie False, la requête d'upgrade est
# servie comme un GET HTTP ordinaire — et comme le token du WebSocket voyage en
# query param faute d'en-tête possible sur `new WebSocket()` (CLAUDE.md §3.6),
# c'est `_require_api_token`, qui ne lit que `Authorization: Bearer`, qui répond.
# Symptôme observé chez un destinataire : « WARNING: No supported WebSocket
# library detected » au démarrage, puis 401 sur /ws/chat. Le chat, l'Atelier et
# la voix passent tous par là : le paquet démarrait sans rien servir.
#
# CAUSE, et non symptôme : la ligne n'a jamais existé. `chromadb` déclarait
# `uvicorn[standard]>=0.18.3`, dont l'extra `standard` tire `websockets>=10.4` —
# l'implémentation arrivait donc en transitif, par une dépendance qui n'avait
# rien à voir avec les WebSockets. Son retrait le 2026-08-13
# (docs/remplacement-vectoriel.md) l'a emportée avec la grappe :
# `tools/contraintes-paquet.txt` la liste noir sur blanc parmi les 49 départs, à
# côté de `watchfiles` — et `httptools`, du même extra, est parti dans le même
# lot. Ces deux-là étaient bien du poids mort. Celle-ci ne l'était pas, et rien
# ne les distinguait.
#
# LE TROU N'EST PAS PROPRE À ARM64. Vérifié par carte de dépendances inverse sur
# le poste de dev (x64) : plus rien d'installé ne réclame `websockets` — `openai`
# ne le déclare que sous son extra `realtime`, jamais installé. Il n'y survit
# qu'en orphelin de l'époque chromadb, ce qui est exactement pourquoi le poste de
# dev ne pouvait pas voir le bug. Un `pip install -r requirements.txt` neuf
# n'installait aucune implémentation WebSocket, sur AUCUNE architecture.
#
# `wsproto` ET NON `websockets` : `wsproto` est du Python pur (wheel
# `wsproto-1.3.2-py3-none-any.whl`, pas un `.pyd` dedans), donc aucune question
# de wheel `win_arm64` à vérifier — la question qui a déjà coûté `chromadb`
# (aucune wheel ARM64) et la voix sur ARM64 (`ctranslate2`, ni wheel ni sdist,
# cf. tools/faire_paquet.py). `websockets` embarque un module d'accélération
# compilé ; sa disponibilité ARM64 n'est pas confirmée, et il n'y a pas de raison
# d'aller la vérifier pour un besoin que le Python pur couvre. Coût transitif :
# ZÉRO — `wsproto` ne demande que `h11<1,>=0.16.0`, déjà dans l'arbre puisque
# `uvicorn` en dépend directement.
# Verrouillé par backend/test_websocket_dependance.py.
wsproto==1.3.2
# Requis par les endpoints à UploadFile/File(...) : FastAPI lève à l'import du
# router si le paquet manque. Il était présent par hasard sur le poste de dev,
# jamais déclaré — `pip install -r requirements.txt` seul (Docker) produisait
# une image qui ne démarrait pas.
python-multipart==0.0.29
# Requis par fastapi.testclient.TestClient (test_auth_surface.py). Arrivait
# jusqu'ici en transitif (ollama, openai) : à déclarer puisqu'on en dépend
# directement.
httpx==0.28.1
ollama==0.6.2
pyyaml==6.0.3
# `chromadb` était ici. Retiré le 2026-08-13, remplacé par `core/vector_store.py`
# (SQLite + numpy) — cf. docs/remplacement-vectoriel.md, étape D. Son retrait
# emporte toute une grappe qu'il déclarait en dépendances directes et
# inconditionnelles, donc impossible à écarter tant qu'il restait : `grpcio`,
# `kubernetes`, `opentelemetry-exporter-otlp-proto-grpc` (et de proche en proche
# `googleapis-common-protos`, tout `opentelemetry-*`, `pypika`, `mmh3`). Mesuré
# par carte de dépendances inverse avant de toucher à quoi que ce soit : aucun
# autre paquet installé ne les réclame. `onnxruntime` en revanche RESTE — et il
# est DÉCLARÉ juste en dessous depuis le 2026-08-26 : il ne survivait ici qu'en
# transitif de `faster-whisper` et `piper-tts` (il n'était pas à chromadb), ce qui
# ne suffisait plus une fois qu'il porte l'embedding. Détail sous la ligne.
onnxruntime==1.26.0
# ── Le moteur d'embedding, depuis le 2026-08-26 ──────────────────────────────
# REMPLACE `sentence-transformers==5.5.1`, qui vivait ici et tirait torch,
# transformers, scikit-learn et scipy : 198,3 Mo de wheels, 843 Mo sur disque,
# pour un usage réel de TROIS appels (`SentenceTransformer(nom)`,
# `.get_embedding_dimension()`, `.encode(..., normalize_embeddings=True)`).
#
# Ce qui a forcé le remplacement n'est pas le poids mais un blocage dur, mesuré
# deux fois à huit minutes d'écart sur la machine ARM64 d'un destinataire :
# **Smart App Control y bloque durablement `sklearn/utils/_isfinite`**, que
# `sentence-transformers` importe SANS CONDITION à son chargement — pour un
# `cos_sim` que `core/vector_store.py` n'appelle jamais, puisqu'il calcule son
# cosinus en numpy. `pip install` réussissait ; l'import plantait, à chaque fois.
# Et scikit-learn est une dépendance inconditionnelle de TOUTES les versions de
# sentence-transformers (vérifié de la 2.7.0 à la 6.0.0) : changer de version
# n'était pas une issue.
#
# MÊME MODÈLE, mêmes vecteurs : `all-MiniLM-L6-v2`, dont le dépôt HuggingFace
# publie déjà l'export ONNX fp32. Mesuré contre l'ancienne pile sur 40 chunks
# réels (dont 37 tronqués à 256 jetons) : cosinus 1.0 pour les 40, écart absolu
# maximal 1,9e-07. Les données vectorielles existantes restent valides — aucune
# réindexation. La tokenisation est reprise en Python pur
# (`core/wordpiece.py`, parité identifiant par identifiant sur 200 échantillons)
# précisément pour ne pas réintroduire un binaire non signé avec `tokenizers`.
#
# DÉCLARÉE ICI, EN DIRECT, ET C'EST LE POINT À NE PAS DÉFAIRE. `onnxruntime`
# était déjà installé sur ce poste — mais en TRANSITIF, par `faster-whisper` et
# `piper-tts`. Or ces deux-là sont retirés des paquets ARM64
# (`HORS_PAQUET_PIP_ARM64` : `ctranslate2` ne publie aucune wheel `win_arm64` ni
# sdist), donc la pile d'embedding aurait dépendu de paquets vocaux absents sur
# l'architecture même qui a motivé le chantier. C'est mot pour mot l'incident
# `websockets` / `uvicorn[standard]` : une implémentation qui arrive par une
# dépendance sans rapport, puis disparaît avec elle. Verrouillé par
# `backend/test_dependances_declarees.py`.
#
# ARM64 : wheel `win_arm64` publiée pour cp311 → cp314 (13,7 Mo en cp312, la
# version du runtime embarqué dans le paquet), et ses trois binaires sont signés
# `CN=Microsoft Corporation` — vérifié SUR la machine cible, où l'import passe en
# 0,08 s et où le vecteur produit est identique bit pour bit à celui du x64.
# Ses dépendances (`flatbuffers`, `packaging`, `numpy`, `protobuf`) sont toutes
# pures ou couvertes : `protobuf` publie un `py3-none-any` qui couvre ARM64.
# **Il n'y a plus de contournement `--index-url download.pytorch.org` à tenir.**
#
# Les 90,4 Mo de poids du modèle ne sont PAS dans cette liste : ils se
# téléchargent au premier usage de la recherche documentaire, vérifiés par
# sha256 — exactement ce que `core/voice.py` fait des 76 Mo de Piper.
# Cf. `core/embedding_install.py`.
# Requis par core/vector_store.py (docs/remplacement-vectoriel.md). Arrivait jusqu'ici en
# transitif (pandas) : à déclarer puisqu'on en dépend directement — même raison
# que python-multipart/httpx ci-dessus.
numpy==2.5.2
pypdf==6.12.2
watchdog==6.0.0
faster-whisper==1.2.1
piper-tts==1.4.2
python-dotenv==1.2.2
google-generativeai==0.8.6
openai==2.38.0
python-docx==1.2.0
# ── Lecture des documents bureautiques pour le RAG (core/rag.py) ─────────────
# Ajoutés le 2026-08-24. `python-docx` était déjà là, et servait DÉJÀ à la
# lecture (pas seulement à l'écriture) — ce qui manquait était le contenu de ses
# tableaux, que `doc.paragraphs` n'inclut pas.
#
# Les quatre paquets de ce lot (les deux ci-dessous + `et-xmlfile` et
# `XlsxWriter` en transitif) publient tous une wheel `py3-none-any` et
# n'embarquent AUCUNE extension compilée — vérifié fichier par fichier, pas
# déduit du nom. Il n'y a donc pas de question de wheel `win_arm64`, celle qui a
# coûté `chromadb` puis la voix sur ARM64.
#
# Coût mesuré sur site-packages : **+6,6 Mo** au total, et **zéro dépendance
# transitive nouvelle** — `lxml` (9,3 Mo) arrive déjà par python-docx, `Pillow`
# (16,2 Mo) par pystray. C'est ce qui rend ce lot bon marché : il réutilise
# l'arbre au lieu de l'étendre.
#
# `XlsxWriter` (1,9 Mo) est du poids qu'on ne choisit pas : python-pptx le
# déclare en dépendance directe pour l'ÉCRITURE de graphiques, dont on ne se sert
# pas. Non écartable tant que python-pptx est là — même situation qu'`onnxruntime`
# à l'époque de chromadb, et la leçon est la même : on mesure avant de purger,
# parce qu'un paquet qui a l'air lié à une fonction qu'on n'utilise pas peut être
# réclamé par autre chose.
#
# AUCUNE conversion externe : pas de LibreOffice, pas d'Office, pas d'appel à un
# binaire. C'est une contrainte du paquet distribué (le destinataire n'installe
# rien), pas une préférence — et `shell=True` est de toute façon interdit
# (CLAUDE.md §6).
#
# Ce qui n'est PAS couvert, et ne le sera pas par ces paquets : `.doc`, `.ppt`,
# `.xls`, les formats binaires pré-2007. Aucune des trois bibliothèques ne les
# lit — elles ne parlent que l'OOXML zippé. Les ajouter à SUPPORTED_EXTENSIONS
# donnerait une erreur à l'ouverture au lieu d'un refus honnête à l'upload.
python-pptx==1.0.2
openpyxl==3.1.5
pandas==3.0.3

# ── Extraction du contenu réel des pages web (core/webcontent.py) ───────────
# Phase 4 de la recherche @web : remplace le snippet DuckDuckGo (~150
# caractères, écrit par DDG) par le texte principal réel des pages les plus
# pertinentes, avant reclassement par similarité avec le modèle d'embedding
# du RAG.
#
# `trafilatura` a été écarté APRÈS vérification, pas par principe : il tire
# `dateparser`, qui déclare `regex` en dépendance directe. `regex` est
# précisément l'un des deux binaires (`.pyd` non signé) dont le blocage par
# Smart App Control sur la machine ARM64 cible est MESURÉ dans ce dépôt (via
# `sentence-transformers`, retiré pour cette raison le 2026-08-26 — cf.
# `core/embedding.py`). Le réintroduire par une autre porte aurait recréé un
# risque déjà vécu, pas hypothétique.
#
# `readability-lxml` (portage Python de l'algorithme Readability de Firefox)
# ne dépend que de `lxml` (DÉJÀ dans l'arbre — `python-docx` le tire depuis le
# lot bureautique ci-dessus), `cssselect` et `chardet`, tous deux purs Python.
# Vérifié par installation réelle sur ce poste (Windows) : AUCUN binaire
# nouveau, `lxml` mis à part — qui, lui, publie une wheel `win_arm64` pour
# cp312 et est déjà accepté dans l'arbre depuis le lot bureautique.
readability-lxml==0.9

# ── Lanceur système : epure_tray.py, PAS le backend ──────────────────────────
# Ni `main.py` ni `core/*` ne les importent — le backend démarre sans. Elles
# sont déclarées ici parce que requirements.txt est le seul fichier de
# dépendances Python du dépôt et que `python epure_tray.py` est la façon
# documentée de lancer Épure (README, CLAUDE.md §2) : non déclarées, cette
# commande échouait sur `import pystray` dès le premier poste neuf. Troisième
# dépendance présente par hasard sur le poste de dev, après python-multipart et
# httpx — même symptôme à chaque fois, un `pip install -r requirements.txt`
# complet qui ne suffit pas à faire tourner ce qu'on documente.
#
# Coût assumé : l'image Docker, qui n'exécute jamais le tray, installe deux
# paquets pour rien — 15,6 Mo sur disque, mesurés, dont 15,4 pour Pillow. Les
# en sortir demanderait un second fichier de dépendances, donc deux listes à
# tenir à jour pour un unique poste : plus cher que les 15 Mo, et c'est
# précisément le genre d'écart qui recrée le trou qu'on bouche ici.
#
# Sous Linux (job `integration` de la CI, runner sans serveur X) l'installation
# passe : la seule dépendance que pystray y ajoute est `python-xlib`, du Python
# pur — rien ne compile et rien ne réclame d'affichage à l'installation. C'est
# l'IMPORT de pystray qui exigerait un display, et aucun test ne charge le tray.
pystray==0.19.5
Pillow==12.2.0

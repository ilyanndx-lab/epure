.claude/settings.local.json
# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
*.so

# Virtual environments
.venv/
venv/
env/

# Environment files — never commit secrets
.env
.env.*
!.env.example

# ChromaDB data — ancien index, conservé le temps de la migration (étape C/D de
# docs/remplacement-vectoriel.md), remplacé par vector_db/ ci-dessous.
backend/chroma_db/

# Store vectoriel (core/vector_store.py) — défaut de EPURE_VECTOR_DIR.
# Index dérivé, reconstructible en réindexant les fichiers surveillés, et propre
# à chaque utilisateur : rien à versionner.
backend/vector_db/

# Modèles de synthèse vocale (Piper) — cache, pas du code.
# Le fr_FR-upmc-medium.onnx pèse 76 Mo ; versionné, il faisait à lui seul 71 Mo
# de .git payés par chaque clone. Il est téléchargé au premier usage de la voix
# et vérifié par sha256 (cf. core/voice.py), donc reconstructible à l'identique.
#
# La purge d'historique a eu lieu (docs/purge-onnx.md, commit B) : ces fichiers
# ne sont plus suivis et n'existent dans aucun commit. Vérifié —
# `git ls-files backend/piper_models` ne rend rien, et `git rev-list --objects
# --all` ne contient plus d'objet .onnx. Cette ligne protège donc bien quelque
# chose, au lieu de décrire un état transitoire qui n'existe plus.
backend/piper_models/

# Cache du modèle d'embedding ONNX (`model.onnx` 90,4 Mo + `vocab.txt`) — défaut
# portable de EPURE_EMBEDDING_DIR, arrivé le 2026-08-26 avec le remplacement de
# `sentence-transformers`. Même nature que `piper_models/` ci-dessus : téléchargé
# au premier usage et vérifié par sha256, donc reconstructible à l'identique, et
# à ne jamais suivre — 90 Mo de binaire dans l'historique d'un dépôt Git ne
# s'enlèvent qu'avec une purge (docs/purge-onnx.md l'a déjà appris une fois).
backend/embedding_model/

# Poids de transcription manuscrite (`encoder_model.onnx` 87,5 Mo +
# `decoder_model.onnx` 30,1 Mo + six fichiers de configuration) — défaut portable
# de EPURE_HMER_DIR, arrivé le 2026-09-07 avec la phase 2 du module `encre`.
# Troisième cache de la même famille que les deux ci-dessus : téléchargé au
# premier usage sur une révision HuggingFace ÉPINGLÉE et vérifié par sha256, donc
# reconstructible à l'octet. À ne jamais suivre, pour la raison écrite plus haut :
# 118 Mo de binaire dans l'historique ne s'enlèvent qu'avec une purge.
backend/hmer_model/

# Exigences dérivées, produites par le job `integration` de la CI (et par
# `tools/faire_paquet.py` si on l'invoque à la main) : `requirements.txt` moins
# `HORS_PAQUET_PIP`. Un artefact, jamais une source — la seule liste que l'on
# tient est `backend/requirements.txt`.
requirements-integration.txt

# Données utilisateur (fiches PDF) — défaut portable de EPURE_FICHES_DIR
/data/
backend/doc_uploads/

# Historique des conversations (runtime, propre à chaque utilisateur)
backend/history/

# Pages d'encre manuscrite du module `encre` — défaut portable de
# EPURE_ENCRE_DIR. Données utilisateur irremplaçables (docs/module-encre.md :
# « stocker l'encre brute, toujours »), donc même régime que history/ : hors
# dépôt, détourné ET surveillé par _test_env.
backend/encre/

# Exemples d'entraînement (encre, LaTeX vérité) du module `encre`, phase 3 —
# défaut portable de EPURE_ENCRE_DATASET_DIR. Chaque exemple porte sa PROPRE
# copie des tracés (survit à la suppression de la page d'origine) : données
# utilisateur irremplaçables, même régime que backend/encre/ ci-dessus.
backend/encre_dataset/

# Export manuel du dataset (tools/export_dataset_encre.py) — artefact de build
# régénérable depuis backend/encre_dataset/, jamais versionné.
dataset_encre_export/

# Config et état par instance (générés au runtime, propres à chaque utilisateur)
# Tout backend/memory/*.json est runtime ; on conserve les règles explicites
# historiques ci-dessous à titre de documentation (couvertes par le glob).
backend/memory/*.json
backend/memory/instance_config.json
backend/memory/modules_state.json

# Versions sauvegardees avant toute ecriture dans le module Code
# (core/codeagent._ecrire_avec_sauvegarde : tool du modele, repli confirme,
# edit_file, enregistrement de l'editeur). Ce sont des fragments du workspace
# de l'utilisateur, jamais versionnes -- le glob *.json ci-dessus, qui ne vise
# que backend/memory/*.json a plat, ne les couvre pas.
backend/memory/code_backups/

# Atelier de modules : bac à sable et sauvegardes (runtime, non versionnés)
backend/modules/_staging/
backend/modules/_backups/
frontend/src/modules/generated/_workshop_check_*/
frontend/src/modules/_workshop_check/

# Modules générés par l'Atelier : personnels, hors dépôt.
# Allowlist plutôt que split de dossiers, pour ne pas refactorer deux fois les
# mêmes chemins (MODULES_DIR de module_workshop.py sera touché par l'isolation).
#
# RÈGLE : tout dossier suivi par git doit figurer ici. Un dossier suivi mais
# ignoré est un piège silencieux — les fichiers déjà suivis continuent de l'être
# (un ignore ne s'applique qu'au non-suivi), donc rien ne casse et rien ne
# prévient, mais tout NOUVEAU fichier qu'on y ajoute disparaît sans un mot. Sept
# dossiers étaient dans ce cas ; contrôle :
#     git check-ignore -q <dossier>/__probe__   # exit 0 = ignoré
#
# Les six installables (code, docs, flashcards, kholle, rangement, reviseur)
# sont partis dans modules-catalogue/, qui est suivi normalement. Leurs lignes
# d'exception ont été retirées DANS LE MÊME COMMIT que le déplacement — sans
# quoi l'allowlist aurait menti sur le contenu du dépôt, et un fichier ajouté
# dans un dossier fraîchement installé aurait disparu sans un mot.
backend/modules/*/
!backend/modules/_atelier/
!backend/modules/admin/
!backend/modules/chat/
!backend/modules/encre/
!backend/modules/hello/
!backend/modules/history/
!backend/modules/settings/

# Un module installé depuis le catalogue est copié ici : il est PERSONNEL à
# l'instance et ne doit pas revenir au dépôt. C'est exactement le même régime
# que pour un module généré par l'Atelier — d'où l'absence d'exception pour les
# six ci-dessus, désormais servis depuis modules-catalogue/.
frontend/src/modules/generated/*/
!frontend/src/modules/generated/encre/
!frontend/src/modules/generated/hello/

# Logs
*.log
*.err
epure_tray.log

# Node
# Sans ancrage : `node_modules/` est ignore A TOUTE PROFONDEUR, pas seulement
# sous frontend/. Un motif qui contient un `/` ailleurs qu'a la fin est ancre a
# la racine du depot, donc `frontend/node_modules/` ne couvrait QUE celui-la --
# et un `npm install` lance depuis la racine y laissait un node_modules/ suivi
# comme du code, que `git status` proposait a chaque fois.
node_modules/
frontend/dist/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
.aider*

# Sorties du code-agent (données perso de runtime, recréé au démarrage)
workspace/

# Ancien lanceur PowerShell, retiré du dépôt (chemins absolus, flm inconditionnel,
# taskkill sur le port 11434). Remplacé par `python epure_tray.py`. Ignoré pour
# que la copie locale de ce poste ne revienne pas par un `git add`.
start.ps1

# Paquets assemblés par tools/faire_paquet.py — artefacts de build, jamais versionnés
dist-paquets/
_scratch_corpus_hmer/
